const API_BASE_URL = 'http://192.168.1.5:3000'; // URL del back, ip donde corre el servicio

export const API_URLS = {
    login: `${API_BASE_URL}/auth/login`, // Ruta para el login
    signup: `${API_BASE_URL}/auth/signup`, // Ruta para el registro

};